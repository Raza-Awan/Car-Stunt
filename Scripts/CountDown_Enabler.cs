using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CountDown_Enabler : MonoBehaviour
{
   /* public CountDown_Manager countDown_Manager;

    // Start is called before the first frame update
    void Start()
    {
        Disable_CountDown();
    }

    // Update is called once per frame
    void Update()
    {
        if (Start_Cam_Animation.isSkipped == true)
        {
            Enable_CountDown();
        }
        else
        {
            Invoke("Enable_CountDown", 5f);
        }
    }

    void Enable_CountDown()
    {
        countDown_Manager.enabled = true;
    }
    void Disable_CountDown()
    {
        countDown_Manager.enabled = false;
    }*/
}
